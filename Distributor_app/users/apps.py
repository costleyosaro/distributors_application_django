from django.apps import AppConfig

class UsersConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'Distributor_app.users'

  # Import signals to make sure they run

    

    
